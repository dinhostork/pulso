# Public example for local development only. Copy to .env and edit as needed.
# EXPO_PUBLIC_-prefixed variables are inlined into the JS bundle at build
# time and are public; never put a secret behind this prefix.
# See README.md ("API base URL") for device/emulator host differences.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
